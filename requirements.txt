# Core ML stack — install torch with CUDA support FIRST (see README.md)
torch>=2.4.0
torchvision>=0.19.0

# Image generation
diffusers>=0.30.0
transformers>=4.44.0
accelerate>=0.33.0
safetensors>=0.4.0

# For Flux models and text processing
sentencepiece>=0.2.0

# GUI — Gradio 6.x
gradio>=6.0.0

# Images / utilities
Pillow>=10.0.0
huggingface_hub>=0.26.0

# Optional: for better performance
# xformers>=0.0.27  # Install separately if needed: pip install xformers --index-url https://download.pytorch.org/whl/cu121